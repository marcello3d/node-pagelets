exports.getRoutes = function(url, routesCallback) {
    // TODO: ajax? engine.io?
}
exports.getData = function(url, model) {
    // TODO: ajax? engine.io?

    // connect to data stream for url. maybe keep track of previously loaded data?
    // get data back
    // call model.applyDelta(delta)

    return function disconnect() {

    }

}